# AI 玩家必须具备的基础能力

- 当前开发优先级是先交付可实际调试的 AI 玩家基础闭环。暂停自动练级、自动游戏任务及其验收的额外扩展；先让 AI 玩家通过提示词和已有工具自行安排这些活动，不能让专用自动化功能阻塞 AI 玩家交付。
- AI 玩家的目标是在游戏里持续生活、表现得像真人。使用 Codex CLI 驱动决策与执行，但不能把单次 Codex 目标完成或进程退出视为 AI 玩家停止运行。
- 外层运行时必须提供持续心跳，定期唤醒 AI 玩家进行决策；在玩家处于启用状态时，单个任务完成后仍须继续调度。心跳不能依赖浏览器保持连接或用户持续发送指令。
- 每次心跳先检查当前任务：有未完成任务则继续推进或检查进度，不重复启动；没有待完成任务时，从预设活动中随机选择一个开始。活动不限于游戏任务，发呆、休息、闲逛、聊天、练级等均可作为任务；发呆也要有持续时间或下次唤醒时间，不能导致调度永久结束。
- 发呆只是预设任务的例子，必须提供较丰富、有实际区别的任务库，覆盖探索、社交与邮件、宠物照顾、物品整理、记忆回顾、日程安排和成长等生活内容。每项须明确执行内容及结束条件，并结合人格、当前状态和已有能力执行，不能用大量改名的发呆凑数。
- AI 玩家必须能互相通信，至少支持游戏内邮件与公屏聊天；收到消息应能进入后续决策上下文，并结合身份与历史记忆处理。心跳不要求每次都发言，避免无意义刷屏。
- AI 玩家必须拥有持久记忆，能记录并检索经历、交往、任务进度与后续打算；记忆应按玩家隔离，并在 Codex 回合结束、进程重启或重新登录后保留。
- AI 玩家必须能够给自己创建、查询、取消定时任务，由运行时持久保存并在到期时唤醒处理；定时任务不能只存在于模型的文字回复中。
- 当前活动、下次心跳和定时任务须可恢复；同一玩家的心跳、消息和定时事件应协调执行，不能并发重复启动同一活动或重复提交游戏操作。显式暂停、停用或人类接管仍须得到遵守。
- 模型异常导致暂停后保持暂停，不在后台自动重新启动。管理员直接点击“启动”时，由运行时确认旧执行已结束、处理遗留检查点并重新观察游戏状态；不再要求单独点击“恢复确认”。未知结果和历史记录必须保留，旧执行未结束时不得重叠启动。
- 本地 AI 执行器的日常接入不依赖 SSH：管理端提供指定玩家的复制运行命令，通过玩家专属接入凭证连接生产；不把模型密钥放入命令，不修改操作者的 `.codex/config.toml`。生产继续保存玩家状态、记忆和审计，同一玩家只能有一个执行器。
- 上述能力属于 AI 玩家第一版的必备实现与验收要求，不是可选增强。须验证单个目标完成后的持续运行、空闲活动选择、双向邮件与公屏交流、重启后的记忆与定时任务恢复，不能只以一次模型调用成功作为完成依据。
- 自动练级、自动完成游戏任务仍是优先供人类玩家使用的通用能力，应能独立于 AI 玩家和模型运行；AI 玩家通过共享能力接入。

# 客户端维护范围

- 当前客户端统一使用 `client/web`，桌面与手机均通过浏览器访问；客户端功能、界面和移动端适配都在该目录维护。
- `client/mobile` 的 Godot 原型已清理，不再恢复或新增独立移动客户端。
- `runtime/legacy-client` 等 legacy 目录中的旧客户端仅作为历史版本，供协议、行为及资源格式参考，不作为当前客户端开发入口。此规则仅针对客户端；`server/legacy` 中的现用服务端仍按项目需要维护。
- Web 资源提取与导入所需的公共解码器位于 `tools/asset_cooker.py`；相关工具复用该模块，不依赖已清理的客户端目录。临时提取、分析及转换产物写入 `build/`。
- 核查或导入宠物、骑乘、精灵及地图资源时，使用项目技能 [stoneage-client-assets](.agents/skills/stoneage-client-assets/SKILL.md)；白虎的已验证来源、映射和导入步骤见该技能的参考文档。

# 原版参考目录

- 用户指定的 8.5 原版客户端参考目录为 `~/Sources/github.com/anson1788/stoneage`。核查 8.5 行为、宠物和骑乘资源时，优先检查该完整目录。
- 核查须覆盖客户端源码、服务端源码归档、Lua 脚本及配套配置；不能仅因客户端静态表没有条目，就认定原版不支持某项功能。
- 该目录只读；提取、分析及转换产物写入当前项目的 `build/`。源码中的资源编号与实际动画文件是否存在须分别核实。
- 已下载的原始客户端 BIN 长期保存在 `vendor/client-resources/`（Git 忽略），不得作为临时下载垃圾清理。新养老客户端资源位于 `vendor/client-resources/xinyanglao-2026-09-14/`，来源与 SHA-256 见其 `README.md`、`manifest.json`；白虎骑乘图号为 `110048–110059`，位于 `path/ride-3-1.82-110000-110105/`。
- 新养老客户端完整主程序也保存在上述目录的 `stoneage.exe`，用于离线资源格式分析；同样长期保留，不得随 `build/` 清理删除。其来源、大小及 SHA-256 已登记在同目录 `manifest.json`。
- 白虎原始定位已解码并验证：同组 `adrn.decoded.bin`、`adrn-decoding-provenance.json` 长期保留，全部 27,840 条记录与 `real.bin` 一致。后续导入直接复用已验证的表，不要重复寻找动画或猜测偏移；启动配置参数不得写入公开文件。

# 运维访问说明

- 生产 StoneAge 主机 IP 需要向用户询问，不得猜测或使用示例地址；用户在本次会话提供后可持续使用，无需重复询问。
- SSH 私钥仅引用本机路径 `~/.ssh/id_ed25519_sa`，不得复制私钥内容到仓库。
- 登录方式：`ssh -o IdentitiesOnly=yes -i ~/.ssh/id_ed25519_sa root@<用户提供的生产IP>`
- 排障优先执行只读日志和容器状态检查；重启、部署或配置修改须取得用户当次明确授权。

# 发版原则

- 必须先将代码、配置变更和版本 tag 推送到 GitHub，确认对应 GitHub Actions 检查及镜像发布成功，再在服务器上修改部署配置、拉取该 tag 的镜像并部署。
- 不得先在生产服务器临时修改代码或配置，再补推 GitHub。生产部署必须对应已发布的 GitHub 版本。
- 服务器发布使用项目入口 `bin/stoneage`：`check` 校验、`pull` 拉镜像、`deploy` 部署、`status` 查看状态。镜像已拉取时使用 `bin/stoneage deploy --no-image-update`；不要绕过入口直接用 Docker Compose 发布，以免漏掉项目仓库认证和部署检查。
- 部署顺序固定为：服务运行期间先 `bin/stoneage check`、`bin/stoneage pull`，拉取成功后执行 `bin/stoneage deploy --no-image-update`，由入口完成旧容器停止与新容器启动，最后 `bin/stoneage status`。不得提前手动停服等待下载。
- 只更新程序镜像、未改资源时不做备份，不得为备份延长停服时间；资源变更的备份需求另行明确。保留旧版本镜像以便回退。
- 严格使用 `bin/stoneage`，禁止临时拼装 SSH/脚本编排停服、备份、部署或回滚。部署流程需要调整时，只能在仓库的 `bin/stoneage` 入口修改、提交并发布后使用，不得在生产现场另造流程。
- 部署后核验实际镜像版本、容器健康状态与相关功能。

# GitHub Actions 额度不足时的发版流程

- 不得擅自改用用户本机的自托管 runner，也不得在本机下载大型构建镜像或执行镜像构建，以免消耗用户流量。严格执行用户指定的发版方式；该方式受阻时报告具体原因，不得自行替换方案。
- 用户已授权以下处理方式：执行已获授权的发版任务时，如果 GitHub Actions 因私有仓库额度不足而无法运行，可将原仓库临时从 private 改为 public，执行 Actions 和发版，完成后再恢复 private；无需为这一流程重复请求确认。
- 临时公开前，必须确认将公开的代码、全部 Git 历史、Release 附件及 Actions 日志和产物已完成敏感信息检查与清理，不得暴露账号或凭据；保留通用配置地址和示例。
- 保持原仓库名称不变，先记录并确认仓库原状态为 private，再切换为 public，重新触发受阻的 Actions，核验对应版本检查通过且镜像发布成功。
- 后续部署仍严格遵守上述发版原则，通过 `bin/stoneage` 完成校验、拉取、部署和状态核验，不得因临时公开而跳过检查或另造部署流程。
- 发版与核验完成后，立即将仓库恢复为 private，并通过 GitHub API 核验实际可见性；失败、取消或中断时也必须执行恢复，不得只在成功路径恢复。
- 如果公开后 Actions 仍因账单或其他原因无法运行，恢复 private 并报告实际阻塞原因，不得反复切换可见性；如果恢复 private 失败，立即报告并继续处理恢复，不能将任务标记为完成。

# 客户端图片缓存规则

- 图片资源使用配置的 CDN 固定路径，不得把共享图片的地址替换为临时 `blob:` URL。
- 加载图片必须先复用有效本地缓存；缓存命中时不得请求 CDN，只有缺失或失效时才下载并缓存。
- 不得为统计下载进度、预加载或重复渲染而重复下载同一图片；旧本地缓存也应保留复用。
